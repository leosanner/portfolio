---
name: Commit after each step
description: User wants a separate commit after each logical unit of work, and docs updated after completing a full feature
type: feedback
---

Commit after each logical step, not in batches. Update docs (spec, changelog) after completing a full feature/phase.

**Why:** User explicitly asked for this workflow — commit per step, docs at the end.

**How to apply:** After each implementation step (e.g., one route, one page), commit immediately. After all steps in a feature are done, update project_spec.md milestones and changelog.md.
